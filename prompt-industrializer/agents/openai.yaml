interface:
  display_name: "Prompt Industrializer"
  short_description: "Turn simple prompts into reusable YAML rule prompts"
  default_prompt: "Use $prompt-industrializer to convert this simple prompt into an industrialized YAML rule prompt."
